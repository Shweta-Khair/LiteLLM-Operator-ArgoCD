apiVersion: auth.litellm.ai/v1alpha1
kind: VirtualKey
metadata:
  name: app-service-key
  namespace: litellm
spec:
  keyAlias: app-service-key
  models:
    - llama3.2-1b
  maxBudget: "10"
  budgetDuration: 1h
  connectionRef:
    instanceRef:
      name: litellm-prod
      namespace: litellm
